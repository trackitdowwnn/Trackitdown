# Learn more: https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# ---------------------------------------------------------------------------
# Dependencies
# ---------------------------------------------------------------------------
node_modules/

# ---------------------------------------------------------------------------
# Environment / secrets
# Real secrets live in .env (gitignored). The .env.example template IS
# tracked — see docs/SECURITY_AND_TRUST.md.
# ---------------------------------------------------------------------------
.env
.env.local
.env.*.local

# Android push (FCM) credentials — features/notifications. google-services.json
# identifies the Firebase project used purely as Android's push TRANSPORT (no
# Firebase backend, no SDK, no data). The service-account key is a real secret
# and belongs nowhere near the repo — upload it to Expo via `eas credentials`
# and keep the file outside this folder.
google-services.json
google-services*.json
*-firebase-adminsdk-*.json

# ---------------------------------------------------------------------------
# Expo
# ---------------------------------------------------------------------------
.expo/
dist/
web-build/
expo-env.d.ts

# Generated native projects (created by prebuild / EAS)
/ios
/android

# ---------------------------------------------------------------------------
# Metro bundler
# ---------------------------------------------------------------------------
.metro-health-check*

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
coverage/

# ---------------------------------------------------------------------------
# Native signing / credentials — never commit these
# ---------------------------------------------------------------------------
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.pem
*.mobileprovision

# ---------------------------------------------------------------------------
# Logs & debug output
# ---------------------------------------------------------------------------
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# ---------------------------------------------------------------------------
# OS files
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# TypeScript
# ---------------------------------------------------------------------------
*.tsbuildinfo

# Generated legal pages (scripts/export-legal.mjs) — regenerate, never commit:
# they are a build artefact of src/features/legal/lib/legalContent.ts, and a
# committed copy is a second source of truth for a document users agreed to.
dist-legal/
