/**
 * WHAT:  Route file for the Watchlist tab.
 * WHY:   Thin wrapper per docs/ARCHITECTURE.md rule 3 — all behaviour lives
 *        in the watchlist feature's CollectionsGridScreen.
 * LINKS: src/features/watchlist/screens/CollectionsGridScreen.tsx.
 */

import { CollectionsGridScreen } from '@/features/watchlist';

export default CollectionsGridScreen;
