/**
 * WHAT:  Route file for the Watchlist tab.
 * WHY:   Thin wrapper per docs/ARCHITECTURE.md rule 3 — all behaviour lives
 *        in the watchlist feature's WatchlistScreen.
 * LINKS: src/features/watchlist/screens/WatchlistScreen.tsx.
 */

import { WatchlistScreen } from '@/features/watchlist';

export default WatchlistScreen;
