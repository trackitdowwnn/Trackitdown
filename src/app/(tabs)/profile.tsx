/**
 * WHAT:  Route file for the Profile tab.
 * WHY:   Thin wrapper per docs/ARCHITECTURE.md rule 3 — everything lives in
 *        the profile feature's ProfileScreen (which also hosts the dev tools
 *        and tab-bar badge toggles the old placeholder carried).
 * LINKS: src/features/profile/screens/ProfileScreen.tsx.
 */

import { ProfileScreen } from '@/features/profile';

export default ProfileScreen;
