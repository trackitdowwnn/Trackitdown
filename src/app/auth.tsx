/**
 * WHAT:  Route file for the auth screen (placeholder until sign-in lands).
 * WHY:   Thin wrapper per docs/ARCHITECTURE.md rule 3 — onboarding exits to
 *        this named route so its navigation never changes when real auth
 *        replaces the placeholder.
 * LINKS: src/features/auth/screens/AuthPlaceholderScreen.tsx.
 */

import { AuthPlaceholderScreen } from '@/features/auth';

export default AuthPlaceholderScreen;
