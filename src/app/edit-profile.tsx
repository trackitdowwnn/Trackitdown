/**
 * WHAT:  Route file for editing your profile (full screen, outside the tab
 *        group so the tab bar doesn't compete with the keyboard).
 * WHY:   Thin wrapper per docs/ARCHITECTURE.md rule 3.
 * LINKS: src/features/profile/screens/EditProfileScreen.tsx.
 */

import { EditProfileScreen } from '@/features/profile';

export default EditProfileScreen;
